
name = "ncr-drive-car-rentals"
main = "worker/index.ts"
compatibility_date = "2024-01-01"

# This serves your built React application (the ./dist folder)
assets = { directory = "./dist", binding = "ASSETS" }

[vars]
# These vars are placeholders since the frontend uses LocalStorage in this demo
FRONTEND_URL = "https://your-app.pages.dev"
